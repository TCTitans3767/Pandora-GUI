# Project Requirements
This document outlines the requirements for the project.

## GUI Requirements
- The application must have a user-friendly graphical user interface (GUI).
- The GUI should be responsive and adapt to different screen sizes.
- The GUI must include the following components:
   - navigation menu
   - button draggables
   - button editing panel
   - preview area w/ support for notes
   - 5